class ButtonField {
    constructor(text, callback, selected = false) {
        this.text = text;
        this.callback = callback;
        this.selected = selected;
    }
}

export default ButtonField;